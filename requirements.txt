# Fake News Detection System — Dependencies
# Install with: pip install -r requirements.txt

scikit-learn>=1.3.0
joblib>=1.3.0
nltk>=3.8.0
pandas>=2.0.0
numpy>=1.24.0
flask>=3.0.0          # Optional — only needed for --api mode
matplotlib>=3.7.0     # Optional — for visualization in Colab
seaborn>=0.12.0       # Optional — for visualization in Colab
